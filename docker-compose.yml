version: '3.8'

services:
  # PIA VPN - US Florida (Miami)
  vpn-florida:
    image: qmcgaw/gluetun
    container_name: vpn-florida
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Florida
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.10
    ports:
      - "8881:8888"  # HTTP proxy port
    restart: unless-stopped

  # PIA VPN - US California
  vpn-california:
    image: qmcgaw/gluetun
    container_name: vpn-california
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US California
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.11
    ports:
      - "8882:8888"
    restart: unless-stopped

  # PIA VPN - US New York
  vpn-newyork:
    image: qmcgaw/gluetun
    container_name: vpn-newyork
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US New York
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.12
    ports:
      - "8883:8888"
    restart: unless-stopped

  # PIA VPN - US Texas
  vpn-texas:
    image: qmcgaw/gluetun
    container_name: vpn-texas
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Texas
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.13
    ports:
      - "8884:8888"
    restart: unless-stopped

  # PIA VPN - US Chicago
  vpn-chicago:
    image: qmcgaw/gluetun
    container_name: vpn-chicago
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Chicago
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.14
    ports:
      - "8885:8888"
    restart: unless-stopped

  # PIA VPN - US Seattle
  vpn-seattle:
    image: qmcgaw/gluetun
    container_name: vpn-seattle
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Seattle
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.15
    ports:
      - "8886:8888"
    restart: unless-stopped

  # PIA VPN - US Denver
  vpn-denver:
    image: qmcgaw/gluetun
    container_name: vpn-denver
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Denver
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.16
    ports:
      - "8887:8888"
    restart: unless-stopped

  # PIA VPN - US Atlanta
  vpn-atlanta:
    image: qmcgaw/gluetun
    container_name: vpn-atlanta
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Atlanta
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.17
    ports:
      - "8888:8888"
    restart: unless-stopped

  # PIA VPN - US Las Vegas
  vpn-lasvegas:
    image: qmcgaw/gluetun
    container_name: vpn-lasvegas
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Las Vegas
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.18
    ports:
      - "8889:8888"
    restart: unless-stopped

  # PIA VPN - US Washington DC
  vpn-washingtondc:
    image: qmcgaw/gluetun
    container_name: vpn-washingtondc
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Washington DC
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.19
    ports:
      - "8890:8888"
    restart: unless-stopped

  # PIA VPN - US Silicon Valley
  vpn-siliconvalley:
    image: qmcgaw/gluetun
    container_name: vpn-siliconvalley
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Silicon Valley
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.20
    ports:
      - "8891:8888"
    restart: unless-stopped

  # PIA VPN - US East
  vpn-east:
    image: qmcgaw/gluetun
    container_name: vpn-east
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US East
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.21
    ports:
      - "8892:8888"
    restart: unless-stopped

  # PIA VPN - US West
  vpn-west:
    image: qmcgaw/gluetun
    container_name: vpn-west
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US West
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.22
    ports:
      - "8893:8888"
    restart: unless-stopped

  # PIA VPN - US Houston
  vpn-houston:
    image: qmcgaw/gluetun
    container_name: vpn-houston
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Houston
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.23
    ports:
      - "8894:8888"
    restart: unless-stopped

  # PIA VPN - US Salt Lake City
  vpn-saltlakecity:
    image: qmcgaw/gluetun
    container_name: vpn-saltlakecity
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Salt Lake City
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.24
    ports:
      - "8895:8888"
    restart: unless-stopped

  # PIA VPN - US Massachusetts
  vpn-massachusetts:
    image: qmcgaw/gluetun
    container_name: vpn-massachusetts
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Massachusetts
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.25
    ports:
      - "8896:8888"
    restart: unless-stopped

  # PIA VPN - US Oregon
  vpn-oregon:
    image: qmcgaw/gluetun
    container_name: vpn-oregon
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Oregon
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.26
    ports:
      - "8897:8888"
    restart: unless-stopped

  # PIA VPN - US Virginia
  vpn-virginia:
    image: qmcgaw/gluetun
    container_name: vpn-virginia
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Virginia
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.27
    ports:
      - "8898:8888"
    restart: unless-stopped

  # PIA VPN - US Ohio
  vpn-ohio:
    image: qmcgaw/gluetun
    container_name: vpn-ohio
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Ohio
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.28
    ports:
      - "8899:8888"
    restart: unless-stopped

  # PIA VPN - US Michigan
  vpn-michigan:
    image: qmcgaw/gluetun
    container_name: vpn-michigan
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Michigan
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.29
    ports:
      - "8900:8888"
    restart: unless-stopped

  # PIA VPN - US Pennsylvania
  vpn-pennsylvania:
    image: qmcgaw/gluetun
    container_name: vpn-pennsylvania
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Pennsylvania
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.30
    ports:
      - "8901:8888"
    restart: unless-stopped

  # PIA VPN - US North Carolina
  vpn-northcarolina:
    image: qmcgaw/gluetun
    container_name: vpn-northcarolina
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US North Carolina
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.31
    ports:
      - "8902:8888"
    restart: unless-stopped

  # PIA VPN - US Idaho
  vpn-idaho:
    image: qmcgaw/gluetun
    container_name: vpn-idaho
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Idaho
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.32
    ports:
      - "8903:8888"
    restart: unless-stopped

  # PIA VPN - US Alabama
  vpn-alabama:
    image: qmcgaw/gluetun
    container_name: vpn-alabama
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=private internet access
      - OPENVPN_USER=${PIA_USERNAME}
      - OPENVPN_PASSWORD=${PIA_PASSWORD}
      - SERVER_REGIONS=US Alabama
      - FIREWALL_OUTBOUND_SUBNETS=172.22.0.0/16
      - HTTPPROXY=on
    networks:
      vpn_network:
        ipv4_address: 172.22.0.33
    ports:
      - "8904:8888"
    restart: unless-stopped

networks:
  vpn_network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.22.0.0/16
